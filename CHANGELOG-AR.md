# سجل تحسينات الإصدار 1.2.0 — تطبيق مختبر عقلان

مراجعة هندسية شاملة نفّذت **20+ إصلاحاً** عبر 20 ملفاً. كل تغيير موثّق بتعليقات
`FIX / SECURITY FIX / HARDENING` داخل الكود مباشرة.

## إصلاحات حرجة (Critical)

1. **إصلاح انهيار الإقلاع** — `DentalLabViewModel`: كتلة init الأولى كانت تجمع `allShipments`
   (معرّف بعد_init) عبر Main.immediate فيحدث NPE عند كل إقلاع بارد. نُقل المراقب إلى init
   ثانٍ بعد التعريفات.
2. **تشفير PIN بمعيار PBKDF2** — `SecurityUtils`: 210,000 دورة HmacSHA256 بصيغة
   `v2:salt:hash` (بديل SHA1 للأجهزة API 24/25)، مع حذف مسار التحقق بالنص الصريح نهائياً
   وترقية تلقائية للتجزئات القديمة عند أول دخول (`needsRehash`).
3. **إغلاق ثغرة الدخول بأي PIN** — `UserSessionRepository.signInWithLocalPin`: اشتراط تطابق
   اسم المستخدم/البريد (كان يكفي أي PIN صحيح للدخول بأي حساب).
4. **مطابقة قواعد Firestore للمسارات الفعلية** — إضافة `all_shipments` و`labs` و
   `storage_backups`، وقراءة الجهاز لمالكه، ورفع بيانات العيادة للأدوار المالية (كانت
   المزامنة تفشل للجميع).
5. **إزالة انتحال هوية المالك** — `getActiveUserSafe` تعيد هوية «النظام» منخفضة الصلاحية
   بدل SUPER_ADMIN وهمي باسم الدكتور (كان يزوّر سجل التدقيق).
6. **بذر لمرة واحدة** — علم `seed_completed` دائم يمنع إعادة استيراد البيانات التجريبية
   ومحو رموز PIN بعد التصفير، مع حماية المستخدمين عند إعادة البيانات التجريبية.
7. **Upsert حقيقي لربط الأجهزة** — `insertOrUpdateDevice` مفتاحه `deviceId` داخل معاملة +
   `ORDER BY registeredAt DESC` (كانت الصفوف تتكرر والأجهزة المحظورة تبقى داخل التطبيق).

## إصلاحات أمنية

- تجريد `pinCode` من كل النسخ الاحتياطية (Firestore + Storage + JSON) مع حفظ الرموز المحلية عند الاستعادة.
- `allowBackup=false` + قواعد استبعاد صريحة في backup_rules / data_extraction_rules.
- App Check إجباري في الدوال السحابية + كلمات مرور ≥10 خانات + قائمة روابط تحديث HTTPS موثوقة + إخفاء أخطاء الخادم.
- HTTPS إلزامي للبوابات المخصصة/Webhooks + إرسال المفتاح مرة واحدة + إغلاق استجابات OkHttp.
- إزالة النجاح المزيف في النسخ الاحتياطي وإنشاء المستخدمين السحابي.
- النسخ الاحتياطي التلقائي للأدوار المالية فقط.

## سلامة البيانات

- المزامنة ترفع **كل** السجلات بدفعات ≤500 عملية (كانت 100 إرسالية و50 دفعة فقط).
- الاستعادة **تستبدل** داخل `withTransaction` بدل الدمج، مع الحفاظ على رموز PIN المحلية.
- ترقيم إرساليات بلا تكرار عبر `MAX()` بدل `count+126`.
- معاملات ذرّية لكل الكتابات متعددة الجداول + دفتر مخزون يسجل الفرق الفعلي.
- معرفات مستخدمين حقيقية عبر `upsertCloudUser` (إيقاف `currentTimeMillis % 10000`).
- مسح `actualReceivedDate` عند مغادرة حالة «مستلمة».

## الأداء وتجربة الاستخدام

- توليد PDF و QR على Dispatchers.IO/Default (منع ANR) + تنظيف كاش التقارير أسبوعياً.
- استرجاع العملة وأسعار الصرف عند الإقلاع + إضافتها لمصادر `dashboardStats`.
- الحفاظ على تاريخ التسليم الأصلي عند تعديل إرسالية.
- إظهار زر الحذف للمشرف الأعلى، والربط العميق لإشعارات النظام.
- قفل تطبيق حقيقي محفوظ، ومزامنة تلقائية محفوظة، ومؤشر اتصال دقيق.
- مُنسّقات تاريخ آمنة للخيوط (ThreadLocal) وإزالة مستمع Firestore في onCleared.
- `isAppVersionSupported` بمقارنة إصدارات فعلية + تحديث `CURRENT_APP_VERSION` إلى 1.2.0.

## ملفات أخرى

- `app/build.gradle.kts`: الإصدار 1.2.0 / versionCode 3.
- حذف 4 ملفات APK قديمة (14MB) من المستودع.

## توصيات لم تُنفّذ (الخطوة التالية)

تقسيم God ViewModel (2000+ سطر)، تمكين `room.schemaLocation` وكتابة Migrations فعلية بدل
التصفير التدميري، إضافة فهارس، معرّف Google OAuth حقيقي، نقل القائمة البريدية إلى
`authorized_users`، SQLCipher، EncryptedSharedPreferences، نقل بوابات الرسائل إلى Cloud
Functions، اختبارات ViewModel وقواعد Firestore الفعلية، وتمكين R8.
