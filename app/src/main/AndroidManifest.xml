<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!--
      android:allowBackup="false"
      كان "true" مع ملفي قواعد نسخ فارغين تماماً، أي أن قاعدة بيانات المركز
      كاملة — أسماء المرضى، الإرساليات، الحسابات المالية، وحسابات المستخدمين —
      كانت تُنسخ إلى Google Drive الخاص بحساب الجهاز وتُنقل عبر "نقل بيانات
      الجهاز"، بدون تشفير من طرف التطبيق وبلا علم المركز. بيانات صحية للمرضى
      لا يجوز أن تخرج من الجهاز عبر مسار لا يتحكم به المركز.

      android:excludeFromRecents / FLAG_SECURE تُطبَّق برمجياً في MainActivity
      لمنع ظهور بيانات المرضى في لقطة شاشة قائمة التطبيقات الأخيرة.
    -->
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
